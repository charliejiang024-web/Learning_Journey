垃圾清理器的需求：

我们这个程序要干嘛？
1. 清理指定文件夹下的垃圾文件
2. 支持后缀清理 .log .tmp .bak
3. 支持递归清理 子文件夹也要清
4. 删除前要提示确认，防止误删
5. 记录删除日志
6. 统计一共删了多少个

用文字先表达出来想要做什么，再写代码

我自己的分类？
我自己想到的是要有一个函数先能清理一个文件夹下的垃圾，也就是实现第一步
然后用装饰器去实现2,3,4,5,6，但是好像不太对劲，扯上了模块划分，那我怎么知道哪个需求写哪个，
我很混乱。

=============================
ai回答：
1. 和文件相关的：遍历，删除，判断文件 file_utils
**我自己的陈述**：需求是不是都跟文件相关，那就得有个文件.py，也就是file_utils
我先把这个文件建出来，想一下这个文件包含哪些需求：1,2,3
那么这些需求会在该文件实现。
再看看4,5,6，得有个日志相关的文件：
2. 和日志相关的：打印日志，记录 logger_config
4,5,6是不是都跟日志相关，那就得搞一个跟日志相关的文件，ok，这些都是我自己如果做的话能看出来的
之后的在仔细看一下需求，能不能推出来？按照我的简单脑子可能就直接做了
3. 和规则相关的：哪些是垃圾 rules
既然2是后缀清理，那我是不是要再定义一个规则文件？可是难道我不能直接在file_utils里写吗？？
ai回答：总的来讲，规则未来可能会变化，但是文件的处理大概率是一样的，这就是我们为什么要有rules.py
方便我们来未来增删改查，一个例子：
我们现在是需要清理后缀，未来有可能我需要比如：
1）小于1kb的文件、2）7天前的文件、3）以~为开头的临时文件、4）空文件、5）自定义后缀
而file_utils里的东西却是一成不变的，比如：
1）遍历文件夹、2）删除文件、3）拼接路径、4）判断是否是文件、文件夹
4. 装饰器工具：统计，确认 decorator
这个也就好理解了 - 不可能每次修改都要再修改一遍file_utils
5. 主程序调用上面所有 main
这个好像都是常见的了，那么现在全都想通了、
